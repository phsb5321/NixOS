# Ignore age keys (NEVER commit private keys)
*.txt
keys/

# Sops-encrypted YAML files are safe to commit (they contain only ciphertext)
# Only truly unencrypted files should be listed here explicitly
!*.yaml
